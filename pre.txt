我们现在在对SAGA-SR: SEMANTICALLY AND ACOUSTICALLY GUIDED AUDIO SUPER-RESOLUTION 是一个递交给2026 ICASSP的论文，进行复现
但是在复现的过程中出现了一些问题：
请你进行检查，检查无误后在后面的【】打勾
按照数据流动的方式进行检查，检查的项目包括：
1. 音频的维度对齐，尤其是维度的变换，长度一致等
2. 输入到模型中是否正确，是否符合论文中提到的技术路线。
下面检查如下项目：
1. 数据预处理【✔】
   - `SAGASRDataset` 新增 `num_samples` 参数，并在训练/验证中显式传入 `65536`，保证与 VAE/DiT 的 `sample_size` 完全对齐，避免浮点取整造成的 65268 长度问题。
2. 模型内部流动【✔】
   - 训练阶段会冻结 VAE/T5，仅更新 DiT 与 roll-off 条件器；roll-off 条件通过 cross-attn 与 prepend token 注入，并且 Euler 采样 wrapper 已同步推理流程，使用 roll-off global 生成 prepend token。
3. 输出的后处理【✔】
   - 训练验证与推理阶段均调用 `_low_frequency_replace`，按每个样本的低分辨率 roll-off 截止频率替换对应频段，确保完整保留原始低频。